class CSVPipeline():
    def __init__(self, csv_path):
        self.csv_path = csv_path

    def process_rois():
        #seg = Segmentation(dicom_folder)
    
        for linha in csv:
            paciente = linha.paciente
            Segmentation.process(dicom_folder, paciente)
            #seg.process(paciente)


